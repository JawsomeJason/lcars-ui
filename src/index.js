/**
 * LCARS UI Component Library
 * Main entry point - exports all components
 */

// Export components (will be added as components are implemented)
export * from "./components/index.js";

// Export utilities
export * from "./utils/theme-manager.js";

// Note: Polyfills are exported separately via "./polyfills" entry point
// Users can import them with: import "lcars-ui/polyfills"
